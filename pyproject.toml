[project]
name = "nd-api-to-gui"
version = "0.1.0"
description = "Print mapping between Nexus Dashboard (ND) REST API keys and ND GUI field names."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "mypy>=1.18.2",
    "requests>=2.32.5",
    "types-requests>=2.32.4.20250913",
]
